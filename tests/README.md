# tests

Testes de integração/E2E que cobrem mais de um workspace (`apps/web` + `apps/api`). Testes unitários específicos de cada pacote vivem dentro do próprio pacote.

Status: vazio, aguardando Bloco 4 em diante.
