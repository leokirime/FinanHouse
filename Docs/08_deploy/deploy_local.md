# Deploy Local

> Projeto: FinanHouse · Atualizado em: 2026-07-25

## 1. Objetivo

Permitir que qualquer pessoa (ou agente) suba o projeto localmente do zero, sem precisar perguntar a alguém da equipe.

## 2. Pré-requisitos

Versões de runtime, ferramentas e serviços externos (banco local, cache) necessários antes do primeiro `install`.

| Ferramenta | Versão mínima |
|---|---|
| _..._ | _..._ |

## 3. Variáveis de Ambiente

Lista das variáveis necessárias para rodar localmente — detalhe completo em `Docs/03_contracts/contrato_variaveis_ambiente.md`.

_..._

## 4. Passos

1. Clonar o repositório.
2. Instalar dependências: `_..._`
3. Configurar variáveis de ambiente: `_..._`
4. Rodar migrações (se aplicável): `_..._`
5. Iniciar a aplicação: `_..._`

## 5. Build

Comando de build local (quando aplicável a esse contexto) e o que ele gera.

_..._

## 6. Como Verificar que Funcionou

O que checar (URL local, comando de health check, log esperado) para confirmar que a aplicação subiu corretamente.

_..._

## 7. Problemas Comuns

Ver `Docs/08_deploy/troubleshooting.md` para a lista completa.

## 8. Decisões Pendentes

_..._
